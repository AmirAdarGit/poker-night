VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
# Lemon Squeezy hosted-checkout URL for the one-time Pro unlock (optional —
# leave blank to hide the Buy button until the product is live).
VITE_LS_CHECKOUT_URL=
# Tip-jar / "buy me a coffee" URL shown in the user menu (optional — leave blank
# to hide the menu item).
VITE_TIP_JAR_URL=
